version: 2
jobs:
  build:
    docker:
      - image: circleci/node:14
    steps:
      - run: echo "done"

workflows:
  version: 2
  noop:
    jobs:
      - build:
          filters:
            branches:
              ignore: gh-pages
