version: 2
jobs:
  build:
    steps:
      - run: echo "done"

workflows:
  version: 2
  noop:
    jobs:
      - build:
          filters:
            branches:
              ignore: gh-pages
